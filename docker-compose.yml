services:
  database:
    image: container-registry.oracle.com/database/express:latest
    container_name: oracle-db
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PWD=HasloToMaslo
    volumes:
      - oracle_data:/opt/oracle/oradata

volumes:
  oracle_data: